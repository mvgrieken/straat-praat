# Straat-Praat Environment Variables
# Kopieer dit bestand naar .env.local en vul de waarden in

# =============================================================================
# VERPLICHTE VARIABELEN (Client-side)
# =============================================================================

# Supabase Configuration
# Je Supabase project URL (vind je in Project Settings > API)
EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous Key (publieke key, veilig voor client-side)
# Vind je in Project Settings > API > Project API keys
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# =============================================================================
# OPTIONELE VARIABELEN
# =============================================================================

# Platform (automatisch ingesteld door Expo)
EXPO_PUBLIC_PLATFORM=web

# Development mode (true voor development, false voor production)
EXPO_PUBLIC_DEV=true

# AI Service Configuration (optioneel)
# Voor AI-vertalingen en content generatie
EXPO_PUBLIC_OPENAI_API_KEY=sk-...
EXPO_PUBLIC_AI_SERVICE_URL=https://api.openai.com/v1

# =============================================================================
# BELANGRIJKE OPMERKINGEN
# =============================================================================

# 1. EXPO_PUBLIC_ prefix is verplicht voor client-side variabelen in Expo SDK 49+
# 2. Alleen publieke keys mogen hier staan (zoals SUPABASE_ANON_KEY)
# 3. NOOIT server secrets hier plaatsen (zoals SUPABASE_SERVICE_ROLE_KEY)
# 4. Dit bestand wordt NIET gecommit naar Git (zie .gitignore)
# 5. Voor Netlify: voeg dezelfde variabelen toe in Site Settings > Environment

# =============================================================================
# NETLIFY DEPLOYMENT
# =============================================================================

# Ga naar je Netlify site dashboard:
# 1. Site settings > Build & deploy > Environment variables
# 2. Voeg de EXPO_PUBLIC_ variabelen toe (zonder quotes)
# 3. Clear cache and redeploy

# Voorbeeld Netlify environment variables:
# EXPO_PUBLIC_SUPABASE_URL = https://your-project-id.supabase.co
# EXPO_PUBLIC_SUPABASE_ANON_KEY = eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# EXPO_PUBLIC_PLATFORM = web
# EXPO_PUBLIC_DEV = false
